import 'package:app_library/models/projects/projects_model.dart';
import 'package:flutter/material.dart';

class OverviewProjectScreen extends StatelessWidget {
  OverviewProjectScreen({Key? key}) : super(key: key);
  final List<Project> loadProjects = [];

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
